//---------------------------------------------------------------------------
#include "MainUnit.h"
//---------------------------------------------------------------------------
void main(void)
{
  if(OpenDialog1->Execute()){
    FBEMInput.SetMemo(Memo1);
    FBEMInput.ReadFile(OpenDialog1->FileName.c_str());
    TTHMBEM *FBEM = new TTHMBEM(ProgressBar1,Label1,FBEMInput);
  }
}                                                                                                                                                                                                                            

