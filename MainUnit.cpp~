//---------------------------------------------------------------------------
#include "MainUnit.h"
//---------------------------------------------------------------------------
int main(void)
{
    char fname[256];
//    printf("Data file: \n");
//    gets(fname);
//    if(strlen(fname)==0)
      strcpy(fname,"THM_TeST2.dat");
    if(FBEMInput.ReadFile(fname)){
      TTHMBEM FBEM(FBEMInput);
      FBEM.Execute();
    }
#ifdef __BCPLUSPLUS__
    getc(stdin);
#endif
    return 0;
}

